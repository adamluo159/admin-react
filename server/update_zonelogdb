#!/bin/sh
#
DATABASE=$1
IP=$2
SQLNAME="zonelogdb"

mysql -h $IP -f -u root -pmaster -e "create database if not exists ${DATABASE} character set 'utf8';"
mysql -h $IP -f -u root -pmaster -B  ${DATABASE} < ${SQLNAME}.sql
mysql -h $IP -f -u root -pmaster -e  "show databases" | grep -w $1
