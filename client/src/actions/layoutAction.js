import {actionCreator} from '../utils/utils'
export const layoutActions = {}
const mapLayout = {
    "selectMainlayout": actionCreator(layoutActions, 'SELECT_MAINLAYOUT_KEY'),
}
export default mapLayout